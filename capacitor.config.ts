import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'softer_styles',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
